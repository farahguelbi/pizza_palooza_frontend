import 'package:flutter/material.dart';

class Addscreen extends StatelessWidget {
  const Addscreen({super.key});

  @override
  Widget build(BuildContext context) {
return const Center(
      child: Text('Add Screen', style: TextStyle(fontSize: 18)),
      ); 
  }
}